create table table_name (
     column1 datatype,
     column2 datatype,
     column3 datatype
     );     
create table employes (
      id number, 
      ename varchar(10),
      add varchar(5)
      );
