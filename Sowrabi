CREATE TABLE student (
      roll_no number,
      student_name varchar(10),
      dept varchar(5)
      );
INSERT INTO student (roll_no,student_name,dept)
     VALUES (1,'Abi','cse');
INSERT INTO student (roll_no,student_name,dept)
     VALUES (1,'Bala','cse');
INSERT INTO student (roll_no,student_name,dept)
     VALUES (1,'Chandru','cse');
SELECT COUNT(student_name)
    FROM student;

My aim is to become a software engineer.I need to be unique from others in all aspects.
